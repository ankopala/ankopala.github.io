# 项目长期记忆 — my-blog

## 关键事实（避免再犯错）

- **PaperMod 主题的正确仓库地址是 `adityatelange/hugo-PaperMod`**，SSH 地址 `git@github.com:adityatelange/hugo-PaperMod.git`。注意：不是 `adnanhob/PaperMod`（该地址不存在，之前几轮对话误写了这个）。
- 用户 GitHub 用户名：`ankopala`；SSH key 已配置且已添加到 GitHub（`ssh -T git@github.com` 返回 `Hi ankopala`）。
- 用户是代码新手，目标是搭建 Hugo + GitHub Pages 博客，采用「边学边做」方式，偏好自己敲命令而非代跑。

## 环境

- 本机已装：git 2.55.0、Hugo v0.166.0 extended、已有 SSH key（ed25519）。
- 未装：`gh`、credential helper。
- 主题引入方式：submodule（用户接受新手友好的方案，暂不用 Hugo module）。

## 用户偏好

- 笔记/交付文件偏好用**英文文件名**。
- 笔记存放目录：`/Users/anko/WorkBuddy/2026-09-20-11-13-02/`。
