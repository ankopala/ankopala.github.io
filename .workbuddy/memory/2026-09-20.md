# 2026-09-20 工作日志

## 博客搭建学习进度

- 用户初始化了 Hugo 站点（`hugo new site . --format toml`），本地服务器跑通（`hugo server -D`）。
- 学习并记录：git submodule 概念、`--depth=1` 浅克隆、submodule vs Hugo module 对比。
- 笔记文件：`/Users/anko/WorkBuddy/2026-09-20-11-13-02/git-submodule-explained.md`。

## 遇到的问题

- git 通过 HTTPS 克隆时鉴权失败：GitHub 已取消密码认证，需用 SSH 或 PAT。
- 发现 SSH key 已配置且可用（`ssh -T git@github.com` 成功）。
- **PaperMod 主题地址此前误写为 `adnanhob/PaperMod`（不存在），正确为 `adityatelange/hugo-PaperMod`**。已纠正并记入 MEMORY.md。
